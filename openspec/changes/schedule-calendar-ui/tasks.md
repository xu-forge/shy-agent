## 1. 纯函数与单测

- [x] 1.1 周一始月网格 `buildMondayGrid`（或改造现有 `buildGrid`）+ 单测
- [x] 1.2 周范围：`startOfWeek(date)` / `endOfWeek` / 七天数组 + 单测
- [x] 1.3 `occurrenceStatus`（pending/paused/past）+ 单测
- [x] 1.4 `formatScheduleLabel` 频率人话 + 单测

## 2. CalendarView 壳

- [x] 2.1 增加 `viewMode` 周|月；工具栏日期文案、‹ ›、今天、分段控件
- [x] 2.2 按视图计算 expand 的 rangeStart/rangeEnd 并加载
- [x] 2.3 页头「定时任务」+ 副文案 +「+ 新建」

## 3. 周 / 月展示

- [x] 3.1 周视图七列 + 当天高亮 + 实例卡片（时间/本地/标题/状态）
- [x] 3.2 月视图周一网格 + 短条 + 今天高亮；溢出日弱化
- [x] 3.3 点实例打开详情；点空新建；「查看定时任务」进编辑器
- [x] 3.4 详情弹层无「暂停此次」按钮

## 4. 样式与验收

- [x] 4.1 样式对齐参考：圆角容器、周卡片、月短条、详情三列元信息
- [x] 4.2 `typecheck` + 相关单测通过；对照三图自检
