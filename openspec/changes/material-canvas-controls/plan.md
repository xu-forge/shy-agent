# Plan: material-canvas-controls

1. 定义画板交互模式、选择状态和工具栏操作函数，补缩放边界与模式切换测试。
2. 实现内容包围盒和适应画布计算，覆盖空画布、单卡片、嵌套分组和窄视口。
3. 实现底部工具栏及响应式样式，确保工具栏事件不会触发画布拖拽。
4. 实现选择框坐标换算、卡片命中、Shift 追加、清空和选中视觉状态。
5. 执行 `npm run typecheck && npm run lint && npm test`，并手测缩放、适应、平移和框选。
